export async function fetchAirdropStatus(address: string) {
  // Web3 və ya REST API ilə status yoxlamaq üçün funksiya
}

export function formatAmount(amount: number): string {
  return `${amount.toLocaleString()} OCOS`;
}
